# V8

**The eighth generation of artwork. Opened 28.8.2026.**

**The pictures do not live here.** From 28.8.2026 V8 artwork lands in
`markoboskoauroville/ANIMATOR_COLLABORATION`, under `BB_C_1` to `BB_C_8`, because that is where the
watch folder pushes and that is where the animator reads from. The v8 read through lives there too,
in `DOCS/`. This folder stays as a marker so the generation is visible in this repository.

**Why.** `BRAIN_BRAKE` is a 1.73 GB pack. Every 75 MB document written into both repositories cost
150 MB across the two, permanently, because git keeps every blob for ever. One home per file from
here.

`assets/V7/` is not deleted and not edited. It is still the artwork all fifty frames of the cut point
at, it can be referenced and reused, and if an old picture is better than a new one it gets modified
rather than redrawn. See `ARTWORK_INDEX.md`.

**This is the artwork track, not the film document track.** The film PDF is on its own letter, `V4i`,
and the two numbers have nothing to do with each other. See `TAKEOVER.md` section 3.
